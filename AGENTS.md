# 项目规则

## 版本与发布

- 日常开发、修复和本机验收期间，不得修改 `APP_VERSION` 或 `BUILD_NUMBER`。
- 日常开发期间不得新建独立版本的发布说明、Git tag 或 GitHub Release，也不得推送构建产物。
- 只有用户明确提出“发版”、“创建版本”或同等请求，并再次确认发布后，才能更新版本号和构建号、生成发布说明并推送产物。
- 发版时将上一个公开版本之后的所有用户可见变更合并到同一份发布说明中。
