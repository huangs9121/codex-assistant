# 项目规则

## 版本与发布

- 日常开发、修复和本机验收期间，不得修改 `APP_VERSION` 或 `BUILD_NUMBER`。
- 日常开发期间不得新建独立版本的发布说明、Git tag 或 GitHub Release。
- 只有用户明确提出“发版”、“创建版本”或同等请求时，才能更新版本号和构建号，并生成对应的发布说明。
- 发版时将上一个公开版本之后的所有用户可见变更合并到同一份发布说明中。
