# 项目状态

## 当前任务：便捷工具更新与 1.4.1 发布准备（2026-09-08）

- 目标：完善右键工具、多条全局按键映射和 Fn 支持，按用户指定的既有右键页保持界面一致；拟发布 1.4.1。
- 当前状态：功能已安装，本地发布前检查通过；公开发布待最终确认。源码版本仍为 1.4.0 / 19，尚未创建 1.4.1 版本、标签或发布。
- 公开基线：GitHub 最新正式版为 v1.4.0；当前远端 main 为 3ce9d88。之后新增的用户可见变化均属于本次便捷工具更新。

## 关键决定与结果

- 右键工具增加总开关及全局排除清单，默认排除 Blender；用户移除 Blender 后不自动补回。排除针对右键工具，不影响其它便捷工具。原有手势规则保留。
- 全局映射支持多条原始键/目标键、录制和手动设置、说明、独立启停和总开关；重复来源提示冲突。旧左 Command 双击配置迁移为首条。
- Fn 作为目标按修饰键事件处理，配对保持/释放并抑制长按重复。安装版已保存用户指定的 Menu（原 Key 110）→ Fn / 🌐，原有配置保留。
- 用户已确认 Fn 用于微信输入法语音输入；不修改 macOS 系统听写设置。微信实体按键启动和结束效果仍待实测，不把事件检查当成语音输入成功。
- UI 复用同级右键页及共享表格样式，此前三套另行设计均否决。视觉基线和并排对照在 docs/keyboard-mappings/；不再重复要求选图。

## 当前验证与安装

- 发布前复核：263 项项目测试、15 项按键控制器检查、7 项右键控制器检查通过；git diff 检查通过。
- Fn 手动选择与保存已在实际安装窗口验证。更早的多条映射交互另有正式视图隔离验证；实体键盘全局效果仍按 TODO.md 验收。
- 安装入口 /Applications/Codex Quota.app 与根 outputs/Codex Quota.app 的文件清单及校验值一致，版本为 1.4.0 / 19。前一轮安装已重新启动，窗口可直接打开。
- 只保留以上两个长期应用包；已识别的临时验证应用和注册已清理。旧安装均为 outputs/app-archives.noindex/ 内的校验压缩包。
- 无关的规则改动、历史文档、调试文件与工具保留，不混入本次功能提交；未来发布使用干净检出，避免为发布删除现有工作。

## 下一步与证据

- 用户确认微信语音输入实测结果，并最终确认发布 1.4.1 后，更新版本/构建号、生成合并发布说明、提交并推送、构建发布，再运行自动更新链路检查；全部通过才标记发布完成。
- 功能与证据：docs/rightclick-controls.md、docs/keyboard-mappings/verification.md、docs/keyboard-mappings/fn-mapping.md。
- 待验收和发布事项仅维护于 TODO.md；不以整理为由改写 AGENTS.md 或生成记忆。
