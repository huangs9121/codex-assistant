# Codex Quota v1.1.1

本版本修复本地签名构建中的开机启动问题：

- 当 macOS 无法通过 `SMAppService` 识别本地签名 App 时，自动改用用户级 LaunchAgent。
- 点击“开机自动启动”不再表现为菜单消失且没有生效。
- 启动项仍可在菜单中随时关闭。

当前发布使用 ad-hoc 签名且未经 Apple 公证，因此 Gatekeeper 出现安全提醒属于预期情况。
